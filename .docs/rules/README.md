# Rules

Hard rules too granular for `AGENTS.md`. One concept per file, short and imperative. The session-start hook injects every rule into every session, so each file carries a permanent context cost: add one only when it is genuinely non-negotiable.
