# Learnings

Append-only lessons from past sessions, named `YYYY-MM-DD-<slug>.md` with frontmatter (`date`, `tags`, `severity`, `applies-to`). Never delete a learning; supersede it by writing a new one and adding a `superseded-by:` line to the old file's frontmatter.
